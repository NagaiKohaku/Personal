#include "Clamp.h"

///=====================================================/// 
/// 0未満の値を0にクランプする関数
///=====================================================///
float ClampMinZero(float num) {

	if (num <= 0.0f) {
		num = 0.0f;
	}

	return num;
}